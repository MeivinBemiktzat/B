name: Build and Release

on:
  push:
    branches: [ main ]
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET 8 SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Create local tool manifest
        run: dotnet new tool-manifest --force

      - name: Install MonoGame Content Builder tool (local)
        run: dotnet tool install dotnet-mgcb --version 3.8.1.303

      - name: Restore dependencies
        run: dotnet restore EscapeSpaceStation.csproj

      - name: Restore local tools
        run: dotnet tool restore

      - name: Publish portable Windows executable
        run: >
          dotnet publish EscapeSpaceStation.csproj
          -c Release
          -r win-x64
          --self-contained true
          -p:PublishSingleFile=true
          -p:IncludeNativeLibrariesForSelfExtract=true
          -o publish_output

      - name: Rename output to app.exe
        shell: pwsh
        run: |
          Copy-Item -Path "publish_output/EscapeSpaceStation.exe" -Destination "publish_output/app.exe"

      - name: Determine release tag
        id: tag
        shell: pwsh
        run: |
          $tag = "build-$(Get-Date -Format yyyyMMdd-HHmmss)-$($env:GITHUB_SHA.Substring(0,7))"
          echo "tag=$tag" >> $env:GITHUB_OUTPUT

      - name: Create GitHub Release with app.exe
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.tag }}
          name: "Escape Space Station - ${{ steps.tag.outputs.tag }}"
          files: publish_output/app.exe
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
